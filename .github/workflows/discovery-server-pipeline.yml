name: CI
on:
  push:
    branches:
      - main
    paths:
      - 'discovery-service/**'
jobs:
  compile:
    runs-on: ubuntu-latest
    name: Compile
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0


